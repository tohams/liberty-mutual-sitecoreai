# Liberty Mutual - Agent Portal Brand Kit

Source pack version 1.0.0, reviewed September 10, 2026. Native Visual guidance updated and verified September 14, 2026.

The native SitecoreAI Design Brand Kit **Liberty Mutual — Independent Agents** has been created and published from this source pack and assigned to the `liberty-mutual-agent-portal` site. Its native ID is `7dc084e7-9b2a-4b2b-bf66-e3e8fd865156`. The source PDF is `liberty-mutual-agent-portal-brand-knowledge.pdf`.

This verifies native provisioning and site assignment, not retrieval by every AI surface. The final reviewed Agentic artifact, **Cedar Ridge Insurance ABM — Final Reviewed Implementation Package**, contains the corrected audience contract and four validated seed payloads. It was eighth in creation order; the current Artifacts list is newest first. Use the [campaign walkthrough](../agentic-studio/README.md#inspect-the-existing-workflow-and-artifacts) to identify its generic card and verify the content heading. It explicitly used the reviewed brand excerpt because native kit retrieval was unavailable. A separate September 14 native creative-brief run verified retrieval and use of the complete authored Visual guidance, without a supplied excerpt. The [saved text](brand-kit-visual-guidance.txt) includes the palette/logo list guidance and is protected as Non AI editable; the original lists and published knowledge were retained. Top-level color metadata and empty-array projection remain inconsistent, and the run explicitly uses authored text instead. Inspect the [repair proof and repeatable checklist](../agentic-studio/brand-kit-consumption-repair-2026-09-14.md); the earlier ABM excerpt provenance remains unchanged. See the [native capability ledger](../agentic-studio/capability-ledger.md).

For a new tenant, recreate the kit from this source pack, review its generated sections and glossary, and assign it through **Channels → site Dashboard → Localization → Configuration → Brand kit → Save**. Record the destination kit's actual ID and owner; do not assume the current tenant's ID transfers. See [Sitecore's assignment workflow](https://doc.sitecore.com/sai/en/users/sitecoreai/ai-capabilities-in-sitecoreai/assign-a-brand-kit-to-a-site.html).

The PDF contains official public facts and separately identified original portal direction. It is not an official corporate brand manual. `source-manifest.json` records authority and limitations; `observed-web-tokens.json` records exact public CSS observations; `asset-manifest.json` records checksums and presentation-only SVG transformations. All original visible logo path geometry remains intact.

`portal-content-seeds.json` contains 12 resources, 7 family hubs and 4 ABM promotions. Import as CMS drafts and map fields to the Sitecore content model. `internalProvenance` and account-specific campaign input must remain internal. Source references and review dates can be mapped to editor-visible fields and agent-facing resource attribution.

The canonical source SVG snapshots have zero intrinsic dimensions because their original website supplies CSS. Use the standalone SVGs in the app. Those add dimensions and explicit original fill colors, omitting the hidden alternate-theme group and transparent interaction rectangle. Preserve the source files in handoff documentation.

Roboto is provided under the SIL Open Font License 1.1; distribute the font and license together. Official Liberty Mutual logos remain its intellectual property; this user-authorized sandbox use is not a claim of a separate redistribution license. No unverified stock photography is bundled.

Validation: all 13 PDF pages rendered; cover, tables and source register reviewed; text extract checked; links and source IDs validated. The source register intentionally spans two pages. The PDF is an ingestion/reference document, not a tagged accessible PDF publication.
